<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="serial"
            type="com.iam.oneom.core.entities.model.Serial" />

        <import type="com.iam.oneom.pages.main.SerialPageActivity" />

        <import type="com.iam.oneom.core.entities.Util" />

        <import type="com.iam.oneom.core.util.Decorator" />

    </data>

    <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="@{() -> SerialPageActivity.start(context, serial.id)}"
        android:orientation="vertical">

        <ImageView
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_margin="8dp"
            app:posterImageCorner="@{@dimen/episode_item_corner}"
            app:url="@{Util.posterUrl(serial, Decorator.W480)}" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginLeft="88dp"
                android:maxLines="1"
                android:lines="1"
                android:ellipsize="end"
                android:layout_marginTop="8dp"
                android:text="@{serial.title}"
                android:textColor="@color/white"
                android:textSize="20dp" />

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginLeft="88dp"
                android:orientation="horizontal">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@{Util.period(context, serial)}"
                    android:textColor="@color/white"
                    android:textSize="16dp" />
            </LinearLayout>
        </LinearLayout>

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_gravity="bottom"
            android:background="@color/middle" />

    </FrameLayout>

</layout>